   #include<stdio.h> 
    #define SIZE 4
    void transposMatrix(int matrix[SIZE]
    for (int i=0;i<SIZE;i++){
    for (int j=i+1;j<SIZE;j++)
    int temp =matrix[i][j];
    matrix[i][j]=matrix[j][i];
            }
          } 
        }
        void printMatrix(int matrix[SIZE][SIZE]);
        {
          return 0;
